//TODO You want a way to control the max-depth shown.
// Maybe Add a slider at the top/bottom of the screen for it?
// Probably just add a keybinding though, maybe alt+left/right.
// If keybinding, you'll probably want a top-right button that loads the binding tree.
// Allow editing for it.
